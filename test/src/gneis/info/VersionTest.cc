#include <sstream>
#include <gtest/gtest.h>
#include "gneis/info/Version.hh"

namespace gneis {

namespace info {

TEST(Version, Defines)
{
	EXPECT_TRUE(GNEISEMULIB_VERSION_MAJOR >= 0);
	EXPECT_TRUE(GNEISEMULIB_VERSION_MINOR >= 0);
	EXPECT_TRUE(GNEISEMULIB_VERSION_PATCH >= 0);
}

TEST(Version, Get)
{
	const std::vector<int> ver = Version::Get();
	EXPECT_EQ(3, ver.size());
	EXPECT_EQ(GNEISEMULIB_VERSION_MAJOR, ver[0]);
	EXPECT_EQ(GNEISEMULIB_VERSION_MINOR, ver[1]);
	EXPECT_EQ(GNEISEMULIB_VERSION_PATCH, ver[2]);
}

TEST(Version, GetAsString)
{
	std::stringstream s;
	s
	<< GNEISEMULIB_VERSION_MAJOR
	<< '.'
	<< GNEISEMULIB_VERSION_MINOR
	<< '.'
	<< GNEISEMULIB_VERSION_PATCH;
	const G4String ver = Version::GetAsString();
	EXPECT_EQ(s.str(), ver);
}

TEST(Version, Matches)
{
	EXPECT_TRUE(
			Version::Matches(GNEISEMULIB_VERSION_MAJOR, GNEISEMULIB_VERSION_MINOR, GNEISEMULIB_VERSION_PATCH)
	);
	EXPECT_TRUE(
			Version::Matches(GNEISEMULIB_VERSION_MAJOR, GNEISEMULIB_VERSION_MINOR)
	);
	EXPECT_TRUE(
			Version::Matches(GNEISEMULIB_VERSION_MAJOR)
	);
	EXPECT_FALSE(
			Version::Matches(GNEISEMULIB_VERSION_MAJOR + 1, GNEISEMULIB_VERSION_MINOR, GNEISEMULIB_VERSION_PATCH)
	);
	EXPECT_FALSE(
			Version::Matches(GNEISEMULIB_VERSION_MAJOR, GNEISEMULIB_VERSION_MINOR + 1, GNEISEMULIB_VERSION_PATCH)
	);
	EXPECT_FALSE(
			Version::Matches(GNEISEMULIB_VERSION_MAJOR, GNEISEMULIB_VERSION_MINOR, GNEISEMULIB_VERSION_PATCH + 1)
	);
}

}

}
